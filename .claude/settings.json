{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash(git commit*)",
        "hooks": [
          {
            "type": "command",
            "command": "cd /workspaces/sentio-dev-frontend && npx tsc --noEmit 2>&1 | tail -5; if [ $? -ne 0 ]; then echo 'BLOCK: TypeScript errors found. Fix before committing.'; exit 1; fi"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd /workspaces/sentio-dev-frontend && npx tsc --noEmit 2>&1 | grep 'error TS' | head -5; if [ $? -eq 0 ]; then echo '⚠ TypeScript errors detected — fix before continuing'; fi; exit 0"
          }
        ]
      }
    ]
  }
}
